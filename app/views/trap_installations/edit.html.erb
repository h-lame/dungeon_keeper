<h1>Editing Trap Installation <%= scoped_parent_for_title %></h1>

<%= render 'form' %>

<%= actions_menu link_to('Show', [@scope, @trap_installation]),
                 link_to('Back', [@scope, :trap_installations]) %>
